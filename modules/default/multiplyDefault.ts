function multiplyDefault(num1: number, num2: number): number {
  return num1 * num2;
}
export default multiplyDefault;

// export default function multiplyDefault(num1: number, num2: number): number {
//   return num1 * num2;
// }
