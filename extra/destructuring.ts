// Object
const item = {
  name: "Notebook",
  price: 2000,
};

const { name: nam, price: pri } = item;
console.log(nam, pri);

// Array
const car = ["Tesla", 2020, true];
const [brand, year] = car;
console.log(brand, year);
